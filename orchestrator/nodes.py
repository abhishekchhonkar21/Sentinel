# LangGraph node functions: thin wrappers that call agent services via httpx.
# Implement: call_detector(), call_investigator(), call_ranker(), call_narrator(), call_critic().
# Handle retries, timeouts, and structured error propagation back to the graph.
