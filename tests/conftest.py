# Pytest suite for Sentinel: unit tests for schemas, ranker rules, detector stats, eval metrics.
# Integration tests: docker compose up → inject fault → assert pipeline produces grounded report.
# eval/ scripts are also runnable as the primary acceptance test harness.
