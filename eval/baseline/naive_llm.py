# Deliberately naive baseline (Section 7.2): dump raw logs/metrics for incident window
# directly into free LLM with "what's wrong and why?" — no agent pipeline, no structure.
# Used only for comparison; this is the single most persuasive eval artifact in the project.
