# Week 9 deliverable: run Sentinel pipeline vs naive LLM baseline across full fault catalogue.
# Output side-by-side table: top-1/top-3 accuracy, tokens used, wall-clock latency (MTTD).
# Exit criterion: reproducible script that regenerates comparison table from scratch on demand.
# Usage: python eval/run_comparison.py
