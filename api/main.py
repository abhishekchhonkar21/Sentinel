# Public-facing FastAPI API for incident reports (plan: FastAPI API → Grafana / simple frontend).
# Expose: GET /incidents, GET /incidents/{anomaly_id}, GET /health.
# Read from MongoDB incident_reports; optional simple HTML/JSON frontend in api/static/ later.
