# Narrator prompt templates and LLM client wrapper.
# Guardrails: only reference facts present in evidence bundle; no open-ended "diagnose this" prompting.
# Track token usage per call for eval cost comparison vs naive baseline.
