# Application settings loaded from environment variables (.env).
# Define: MONGODB_URI, LLM API keys, agent service URLs, feature flags (Neo4j vs NetworkX).
# Use pydantic-settings BaseSettings so every service reads config the same way.
