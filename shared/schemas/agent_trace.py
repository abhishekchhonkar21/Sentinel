# AgentTrace schema — observability documents (agent_traces collection).
# Fields: anomaly_id, agent_name, input, output, timestamp, latency_ms, token_usage (optional).
# Written at every LangGraph hop so you can query "show me every hop for anomaly X".
