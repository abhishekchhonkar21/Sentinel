# RankedHypotheses schema — Hypothesis-Ranker → Narrator (hypotheses collection).
# Fields: anomaly_id, hypotheses[] where each item has cause, evidence_score,
# supporting_evidence[].
# Output must be deterministic enough for eval harness to compare against fault_catalogue ground truth.
