# Fault injection implementations: bad deploy sim, artificial latency, forced exceptions,
# connection pool starvation, rate-limit misconfiguration, external provider timeout, memory leak sim.
# Each handler calls into the target toy-system service or toggles runtime fault flags.
