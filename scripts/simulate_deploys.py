# Simulate deploy history: write documents to MongoDB deploys collection.
# Include "bad" deploys tied to fault catalogue entries (diff_summary, commit_hash, deployed_at).
# Used by Investigator evidence gathering and hypothesis ranking.
